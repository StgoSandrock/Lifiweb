packages:
  - "apps/*"

# pnpm 11 blocks dependency build scripts unless explicitly approved.
# Allow only the packages already required by the current application.
allowBuilds:
  '@firebase/util': true
  protobufjs: true
  sharp: true
  unrs-resolver: true
